import styled from "styled-components"

const CommentFormContainer = styled.div`
  background-color: var(--surface3);
  position: sticky;
  bottom: 0;
`

const CommentForm = () => {
  return <CommentFormContainer>CommentForm</CommentFormContainer>
}

export default CommentForm
